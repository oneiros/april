= semantic_form_for @time_entry do |f|
  = f.inputs do
    = f.input :project, :collection => grouped_options_for_select(Project.grouped_options, @time_entry.project_id)
    = f.input :date, :as => :string
    = f.input :duration
    = f.input :comment
  = f.buttons
