.page-header
  %h1 New Invoice Step 1: Select Customer 
.row
  .span16
    = semantic_form_for :customer, :url => select_time_entries_invoices_path, :method => :get do |f|
      = f.inputs do
        = f.input :id, :label => "Customer", :as => :select, :collection => @customers
      = f.buttons do
        = f.commit_button "Next"
